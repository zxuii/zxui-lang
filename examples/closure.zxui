fun caller(name) {
    fun things() {
        println(name, "fungsi things()");
        return 0;
    }
    return things;
}

fun tambah(a, b) {
    return a + b;
}

let x = tambah(1, 5);

println("tambah(1,5) = ", x + 5.2);

let c = caller("ini dipanggil dari: ");
c()