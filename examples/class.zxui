class Person {
    fun init(name, age) {
        self.name = name;
        self.age = age;
    }

    fun greet() {
        println("Hello, ", self.name, ". Your age is: ", self.age);
    }
}

class Animal {
    fun init() {
        self.type  = "Dog";
        self.say = "Woof!";
    }

    fun speak() {
        println("The ", self.type, " say ", self.say);
    }
}

class Cat : Animal {
    fun init() {
        super.init();
        self.type  = "Cat";
        self.say = "Meow!";
    }
}

let p = Person("john", 12);
p.greet()

let d = Animal();
d.speak();

let c = Cat();
c.speak();
